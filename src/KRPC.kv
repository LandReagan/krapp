#:import ConnectionScreen ui.connection_widget

<StatusBar>:
    kRPC_version_label: kRPC_version_label
    version_label: version_label
    orientation: 'horizontal'

    height: kRPC_version_label.texture_size[1]

    Label:
        padding: 10, 10
        canvas.before:
            Color:
                rgb: 0.2, 0.2, 0.2
            Rectangle:
                size: self.size
                pos: self.pos
        id: kRPC_version_label
        text: 'UNKNOWN'

    Label:
        padding: 10, 10
        canvas.before:
            Color:
                rgb: 0.2, 0.2, 0.2
            Rectangle:
                size: self.size
                pos: self.pos
        id: version_label
        text: 'UNKNOWN'

<MainWindow>:
    orientation: 'vertical'

    ConnectionScreen:

    StatusBar:
        size_hint_max_y: 30